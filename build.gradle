apply plugin: 'java'

version = '1.0-SNAPSHOT'
group = 'jsr381.experimental.impls'

subprojects {

    group = rootProject.group
    version = rootProject.version

    repositories {
        mavenCentral()
    }
}
